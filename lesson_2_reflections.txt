What happens when you initialize a repository? Why do you need to do it?

-It creates a git metadata file. This allows git commands and tracking to occur within the folder set as the repository holder.

How is the staging area different from the working directory and the repository? What value do you think it offers?

-The staging area is the group of files intended that exist in the working directory for the next commit. The repository is all the commits made so far.

How can you use the staging area to make sure you have one commit per logical change?

-You can compare it to either the working directory (prior to adding) or the latest commit (after adding) and revise the changes made

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

-Setting up a new colour scheme on a webpage
-using different/alternate coding languages for alternate uses
-making different versions, eg for different country usage

How do the diagrams help you visualize the branch structure?

-They help by showing the relationships between commits clearly

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

-It creates a commit that combines all changes in both branches

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

-Pros - a lot faster! conflict resolution identification
-Cons - could possibly make errors based onsimple logic
